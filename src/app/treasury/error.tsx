"use client"; import { Button } from "@/components/ui/button"; export default function Error({error,reset}:{error:Error;reset:()=>void}){return(<div className="flex flex-col items-center justify-center gap-4 p-6 pt-24 min-h-[60vh]"><h2 className="text-xl font-semibold">Failed to load</h2><p className="text-muted-foreground text-sm">{error.message}</p><Button onClick={reset}>Retry</Button></div>)}
